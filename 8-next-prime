public Integer nextPrime(Integer num) {
    Boolean isPrimeNotFound = true;
    While(isPrimeNotFound){
        for(Integer i= 2; i< num-1; i++){
            if(Math.mod(num,i)!=0){
                isPrimeNotFound = false;
                return num;
            }
        }
        num++;
    }
    
}