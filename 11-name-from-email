public String nameFromEmail(String email) {
    String firstName = email.substringBefore('.').capitalize();
    String lastName = email.substringAfter('.').substringBefore('@').capitalize();

    return firstName + ' ' + lastName;
}
