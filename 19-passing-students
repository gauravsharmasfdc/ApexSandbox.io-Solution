public Boolean isPassed(Boolean passedExam, Boolean passedAssignments, Boolean passedProject) {
    Integer counter=0;
    if(passedExam) counter++;
    if(passedAssignments) counter++;
    if(passedProject) counter++;

    return (counter>=2);
}