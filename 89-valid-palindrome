public Boolean isPalindrome(String str){
    String revStr;
    Pattern nonAplhanumaric = Pattern.compile('[^a-zA-Z0-9]');
    Matcher matcher = nonAplhanumaric.matcher(str);
    str = matcher.replaceAll('');
    revStr = str.reverse();
    
    return (str == revstr);
}